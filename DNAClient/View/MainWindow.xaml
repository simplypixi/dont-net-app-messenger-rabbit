<Window x:Class="DNAClient.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DNAClient.ViewModel"
        DataContext="{DynamicResource MainWindowViewModel}"
        Title="MainWindow" Height="300" Width="300">
    <Window.Resources>
        <vm:MainWindowViewModel x:Key="MainWindowViewModel"/>
    </Window.Resources>
    <Grid>
        <StackPanel>
            <TextBlock Height="24" Margin="5,5,5,0" Text="To będzie nasze główne okno" HorizontalAlignment="Center"/>
            <TextBlock Height="24" Margin="5,5,5,0" Text="Recipient"/>
            <TextBox Height="24" Text="{Binding Recipient}" Margin="5,0"/>
            <Button Height="24" Width="100" HorizontalAlignment="Left" Margin="5"
                Content="New conversation"
                Command="{Binding NewConversationWindowCommand, UpdateSourceTrigger=PropertyChanged}" CommandParameter="lol"/>
        </StackPanel>
    </Grid>
</Window>
